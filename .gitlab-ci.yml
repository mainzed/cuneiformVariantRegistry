image: alpine:latest

stages:
  - deploy

pages:
  image: cm0x4d/jsdoc
  stage: deploy
  script:
  - mkdir jsdocs
#  - jsdoc docs/js/*.js -d jsdocs
  - mv jsdocs docs/
  - cp docs/* public/
  artifacts:
    paths:
    - public
  only:
  - master
